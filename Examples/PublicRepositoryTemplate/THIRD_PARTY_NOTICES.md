# Third-Party Notices

The FOnline Engine is consumed as the `Engine/` git submodule and carries its own license and third-party notices. Example-owned files are covered by this repository's `LICENSE` unless a file or asset provenance entry states otherwise.

Every redistributed asset must have one entry in `assets/provenance.json` with its source and license. Do not infer rights from availability on the Internet.
