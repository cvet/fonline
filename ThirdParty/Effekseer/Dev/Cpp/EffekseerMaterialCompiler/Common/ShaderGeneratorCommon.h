#pragma once

#include <Effekseer.h>

#include <string>
#include <vector>

namespace Effekseer
{
namespace Shader
{

std::string GetFixedGradient(const char* name, const Gradient& gradient);

/**
	@brief	Rewrite comparisons assigned to bool (generated by the Compare node) so that
	vector operands are compared by their first component.
	HLSL implicitly truncates a vector comparison to its first component while
	GLSL/MSL reject such code, so extract .x explicitly to keep every backend
	behaving like DirectX.
*/
std::string AdaptBoolCompareExpressions(const std::string& code, const std::vector<std::string>& vectorTypes);

const char* GetGradientFunctions();

const char* GetNoiseFunctions();

const char* GetHsvFunctions();

// TODO : support metal
const char* GetLinearGammaFunctions();

} // namespace Shader
} // namespace Effekseer