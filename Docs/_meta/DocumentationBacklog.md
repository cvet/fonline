# Documentation Backlog

This backlog tracks the planned engine documentation set. It exists so documentation work stays source-grounded: every topic lists the source areas to inspect before writing.

## Status legend

- `planned` — topic is identified but not researched yet.
- `researching` — source/tests/build files are being inspected.
- `drafted` — a first doc exists, but it may need deeper validation.
- `verified` — links and formatting were checked after the latest edit.

## Navigation and maintenance

- `verified` - [README.md](../../README.md), [Docs/README.md](../README.md), [Getting Started](../en/tutorials/getting-started.md), [Embedding FOnline](../en/how-to/build/embedding-project.md), [Build Workflow](../en/how-to/build/), [DocumentationExpansionPlan.md](DocumentationExpansionPlan.md), this backlog, and [DocumentationResearchTemplate.md](DocumentationResearchTemplate.md).
- `verified` — [Source Tree](../en/contributing/source-tree/).
- `verified` — [Architecture](../en/explanation/architecture/), [Applications](../en/reference/applications.md)
  - Verified against `Source/Applications/`, common engine base/entity/script-system owners, client/server/frontend entry points, and application CMake wiring on 2026-08-01.

## Architecture and source navigation

- `verified` — [Architecture](../en/explanation/architecture/)
  - Verified against `Source/Applications/`, `Source/Common/EngineBase.*`, `Source/Common/Entity.*`, `Source/Common/ScriptSystem.*`, client/server/frontend owners, and all current CMake stages on 2026-08-01.
- `verified` — [Source Tree](../en/contributing/source-tree/)
  - Verified against current top-level `Source/` areas, `BuildTools/cmake/stages/Applications.cmake`, and `Source/Tests/` on 2026-08-01.
- `verified` — [Applications](../en/reference/applications.md)
  - Verified against all current `Source/Applications/*.cpp` entry points plus `BuildTools/cmake/stages/Applications.cmake` and `BuildTools/cmake/helpers/Build.cmake` on 2026-08-01.

## Build, generation, and resources

- `verified` — [Build Workflow](../en/how-to/build/)
  - Verified against the strict ten-stage project interface, current application targets, engine CMake/BuildTools entry points, build/test/package wiring, validators, `Source/Applications/TestingApp.cpp`, and test docs on 2026-08-01.
- `verified` — [BuildTools Pipeline](../en/reference/cmake-and-buildtools/pipeline.md)
  - Verified against `BuildTools/Init.cmake`, current staged CMake files, helpers, codegen/package scripts, and build-generation ownership boundaries on 2026-05-18.
- `verified` — [Public Example Repositories](../en/how-to/build/public-example-repositories.md)
  - Re-verified on 2026-08-01 against `Examples/PublicRepositories.json`, the governance overlay, Minimal Project/Minimal Multiplayer/GameplayTestHarness/AiControl fixtures, the generator and focused tests, ADR-0005, package/updater boundaries, external Last Frontier/TLA evidence, and the authenticated state of all four `cvet` staging repositories.
  - The machine registry now records the observation date, default branch, exact remote head, observed Engine pin, and required-check state. Published status requires public visibility, a published remote state, and passing checks. The current audit confirms four private repositories, four source-ready fixtures, one remotely staged candidate, zero published repositories, and no retained run/status evidence on the current template head; its reachable Engine pin predates the validator and must be restaged before publication.
  - The canonical EN/RU guide preserves every former H2/H3 route through [PublicExampleRepositories.md](../PublicExampleRepositories.md), defines the seven-part publication decision, and extracts project practices only through Engine-owned fixtures and evidence gates. Nine focused tests and locale parity raise reviewed mirror coverage to 158/195.
- `verified` - [Model Animation Metadata and Duration](../en/how-to/content/model-animation.md)
  - Re-verified on 2026-08-02 against the source loader, converter, `ModelInfoBaker`, animation-data wire contract, metadata registration, client runtime, script methods, and focused native tests. The audit now explicitly covers finite reciprocal validation, signed 32-bit millisecond range, rejection of positive sub-millisecond cycles that round to zero, one-step alias priority, and the separate bake, visible-pose, and gameplay-timing acceptance layers.
  - Exact Last Frontier evidence records named enum tuples, include templates, `AnimSpeed`, current state/action aliases, and temporary geometry exceptions. Exact TLA evidence keeps bare `AnimEqual` and model-level `Speed` as rejected legacy migration patterns rather than current grammar.
  - The canonical EN/RU guide and [ModelAnimation.md](../ModelAnimation.md) durable pointer preserve every H2/H3 route. Seven focused tests and exact fenced-code parity raise reviewed mirror coverage to 159/195, with 36 required counterparts remaining.
- `verified` - [Sprite Root Motion and Walk Cycles](../en/how-to/content/sprite-root-motion.md)
  - Re-verified on 2026-08-02 against image import/baking, SpriteResource v2 transport, direction sheets, movement interpolation, geometry normalization, `CritterHexView`, `ResourceManager`, and the focused image/geometry tests. The contract now distinguishes valid live multi-hex `HexOffset` values from passability-aware stop-time re-partition and pins the no-jump world-position invariant.
  - The audit corrected the Fallout animation merge path so the base cycle total is subtracted independently on `x` and `y`. Last Frontier's 3D `*_RM` policy is retained only as an ownership boundary; TLA's `raw_ptr` and `LoadAnimation` names are explicit historical evidence, and neither pinned project supplies a current authored 2D root-motion asset from which production offsets can be promoted.
  - The canonical EN/RU guide and [SpriteRootMotion.md](../SpriteRootMotion.md) durable pointer preserve all 19 H2 routes. Eight focused tests, exact fenced-code parity, a clean native build, and full `LF_UnitTests` raise reviewed mirror coverage to 160/195, with 35 required counterparts remaining.
- `verified` — [Baking Pipeline](../en/explanation/content-pipeline/baking.md)
  - Re-verified against `Settings.h/.cpp`, `ScriptsAndBaking.cmake`, the build-hash helper, baker apps/library entry points, all 13 current built-in baker headers/registrations, reports, model/image/particle payload owners, and baker tests on 2026-08-01. The source audit corrected `ModelInfoBaker` order from stale `5` to live `6`, removed duplicated `BakerDataSource` prose, and added source-backed resource-pack composition practices from pinned Last Frontier/TLA configs.
  - The canonical EN/RU explanation and durable legacy pointer preserve all former H2/H3 routes. Five focused tests pin baker names/orders, CMake/codegen dependencies, resource-pack fields, external evidence, and route ownership, raising reviewed mirror coverage to 155/195.
- `verified` — [GeneratedApiAndMetadata.md](../GeneratedApiAndMetadata.md)
  - Verified against `BuildTools/cmake/stages/Codegen.cmake`, `BuildTools/codegen.py`, metadata registration templates/runtime, property/entity model owners, `MetadataBaker`, and metadata/property tests on 2026-05-18.

## Runtime model

- `verified` — [Entity Model](../en/explanation/entity-and-property-model/)
  - Verified against `Source/Common/Entity.*`, `EntityProperties.*`, `EntityProtos.*`, `Properties.*`, `PropertiesSerializer.*`, `ProtoManager.*`, and entity/property tests on 2026-08-01.
- `verified` — [Maps, Movement, and Geometry](../en/explanation/maps-and-movement.md)
  - Verified against `Source/Common/Geometry.*`, `LineTracer.*`, `Movement.*`, `PathFinding.*`, `MapLoader.*`, `Source/Tools/MapBaker.*`, and the current geometry/path/movement/map-loader/map-baker/server-map tests on 2026-08-01. The audit corrected the `MapLoader::Load` signature, documented `EnumerateMaps`, and replaced an indeterminate test note with exact files.
- `verified` — [Networking](../en/explanation/authority-and-networking/)
  - Verified against `Source/Common/NetBuffer.*`, `NetworkUdp.*`, current network settings, `Source/Client/NetworkClient*`, `Source/Server/NetworkServer*`, network tests, and client/server integration tests on 2026-08-01. The audit aligned the five inbound-hardening layers and restored `MaxReorderAhead` to the UDP option inventory.
- `verified` — [Persistence](../en/explanation/persistence/)
  - Verified against `Source/Server/DataBase.*`, `Source/Server/DataBase-*.cpp`, and `Source/Tests/Test_DataBase.cpp` on 2026-08-01, including the current dirty-tree `.oplog` suffix validation and derived `-committed.oplog` path.

## Client, server, and platform runtime

- `verified` — [Client Runtime](../en/explanation/runtime/client.md)
  - Verified against `Source/Client/Client.*`, `ClientConnection.*`, `ResourceManager.*`, client view/sprite/render-target managers, and client tests on 2026-08-01.
- `verified` — [Frontend and Rendering](../en/explanation/rendering/)
  - Re-verified against `Source/Frontend`, `Source/Client`, `Source/Tools`, `Source/Common/Settings.inc`, `BuildTools/cmake/stages/Init.cmake`, and rendering tests on 2026-08-01. The audit adds the missing `AppRender`/`IAppRender` service, pins the exact Null/OpenGL/Direct3D/Vulkan/SDL_GPU backend inventory and compile-time selection order, and records direct Metal as an explicit placeholder rather than an implemented renderer.
  - The complete EN/RU explanation distinguishes Vulkan runtime validation from an optional SDK, adds SDL_GPU acceptance, derives reusable project practices from pinned Last Frontier/TLA configs without copying their settings, and retains every former H2/H3 route through a durable legacy pointer. Seven focused tests raise reviewed mirror coverage to 156/195.
- `verified` — [Server Runtime](../en/explanation/runtime/server.md)
  - Verified against `Source/Server/Server.*`, `Source/Server/*Manager.*`, `Source/Server/Player.*`, `Source/Server/Critter.*`, `Source/Server/Map.*`, `Source/Server/Location.*`, `Source/Server/Item.*`, `Source/Server/ClientDataValidation.*`, `Source/Server/UpdaterBackend.*`, `WorkThread`, `WorkerPool`, server tests, and integration tests on 2026-08-01.
- `verified` — [Client Runtime Split and Updater](../en/explanation/runtime/client-updater.md)
  - Re-verified against `Source/Applications/ClientApp.cpp`, `ClientLib.cpp`, `Source/Client/ClientRuntimeApi.*`, `Source/Client/Updater.*`, `Source/Server/UpdaterBackend.*`, player/connection dispatch, current settings, CMake/package code, native runtime tests, and project updater pipeline evidence on 2026-08-01. The audit corrected the `ptr<Player>` / `const_span<uint8_t>` backend interface and the path-digest cache key, then added executable-payload signing and release acceptance boundaries.
  - The complete EN/RU explanation preserves all former H2/H3 routes through [ClientUpdater.md](../ClientUpdater.md). Seven focused tests pin ABI/protocol generations, host/selector behavior, stateless offset transfer, cache/package mechanics, project evidence, locale completeness, and route ownership, raising reviewed mirror coverage to 157/195.
- `verified` — [Web Build, Packaging, and Browser Debugging](../en/how-to/platforms/web-debugging.md), [Android Build, Packaging, and Device Debugging](../en/how-to/platforms/android-debugging.md), [Native and AngelScript Debugging](../en/troubleshooting/debugging.md)
  - The Web guide was re-verified and migrated to complete EN/RU routes on 2026-08-02 against the pinned Emscripten workspace, Web CMake contract, package grammar/implementation, stock shell/server, canvas/clipboard/IDBFS/main-loop runtime, WebSocket selection, updater capability, support matrix/CI, and exact Last Frontier/TLA evidence. A 2026-08-03 continuation added the Engine-owned Content Showcase native-bake/Web-package/Chromium/WebGL 2 runtime fixture, explicit `FO_EMSDK` precedence, compositor-pixel evidence, and a required `smoke_gated` workflow while preserving project-owned browser/deployment acceptance. The legacy path preserves all headings and focused tests pin the expanded contract.
  - The Android guide was re-verified and migrated to complete EN/RU routes on 2026-08-02 against BuildTools platform/SDK/NDK preparation, package grammar and implementation, Gradle/manifest/activity templates, settings, updater capability, ADB helper, support matrix/CI, and exact Last Frontier/TLA evidence. It now separates ARM build gates, source-capable x86, APK/device/release qualification, debug-key fallback, resource/cache lifecycle, typed server-host launch, and unsupported native self-update; the legacy path preserves all headings.
  - Native and AngelScript Debugging was re-derived and migrated to complete EN/RU routes on 2026-08-02 against build/symbol configuration, cached native-debugger detection and breaks, Natvis/NatJMC wiring, mixed script/native stack interleaving, exception/crash logging and destructive self-test modes, the live TCP/UDP AngelScript endpoint, adapter source/distribution limits, and exact Last Frontier/TLA launch evidence. The guide separates symbols from debug semantics, Engine logs from project-owned dumps, live attach capabilities from mock adapter controls, and loopback development use from unsupported network exposure. `Docs/Debugging.md` preserves all 44 H2/H3 routes, seven focused tests and a mandatory workflow guard pin the contract, and reviewed mirror coverage is 163/195.

## Scripting and tools

- `verified` — [Scripting](../en/explanation/scripting-runtime/)
  - Verified against `Source/Common/ScriptSystem.*`, AngelScript backend/runtime/compiler files, every current core script, native script-method owners, build script-compilation wiring, and current scripting tests on 2026-08-01.
- `verified` — [AngelScript Style and Refactoring](../en/how-to/scripting/style-and-refactoring.md)
  - Re-derived and migrated to complete EN/RU routes on 2026-08-02 against backend file discovery and stable `Sort N`/filename ordering, separate SERVER/CLIENT/MAPPER modules with always-defined `0`/`1` macros, namespace-prefixed mutable-global enforcement, built-in and project-defined direct-call blockers, marker propagation, clang-format 20 plus Engine repairs, all current CoreScripts, and exact Last Frontier/TLA formatter and settings evidence.
  - The guide separates source order from `[[ModuleInit(priority)]]`, requires `#if SIDE` rather than `#ifdef SIDE`, classifies mechanical/structural/behavioral/contract refactors, preserves every canonical H2/H3 route through `Docs/AngelScriptStyle.md`, and raises reviewed mirror coverage to 164/195. Seven focused tests and a mandatory workflow guard pin the contract.
- `verified` — [Script Methods Map](../en/reference/script-api/method-ownership.md)
  - Verified against all 18 current `Source/Scripting/*ScriptMethods.cpp` files and 953 current `///@ ExportMethod` declarations; the 2026-08-01 audit also pins both current Mapper screenshot APIs and separates this route from the generated API index.
- `verified` - [Script Lifecycle and Concurrency](../en/how-to/scripting/lifecycle-and-concurrency.md)
  - Verified against module initialization, callback dispatch, async/yield handling, entity synchronization covers, mutable-state ownership, and teardown paths on 2026-08-01.
- `verified` - [Remote Calls](../en/reference/scripting/remote-calls.md)
  - Verified against client/server send and receive paths, `ClientDataValidation`, `NetBuffer`, remote-call tests, payload limits, collection bounds, full-consumption checks, and validation-before-synchronization order on 2026-08-01.
- `verified` — [Nullability.md](../Nullability.md)
  - Verified against AngelScript nullable suffix parsing, the `ptr<T>`·`nptr<T>` native pointer vocabulary, codegen runtime checks, nullable analyzer tools, parent VS Code/CI task wiring, and nullable/script tests on 2026-05-18.
- `verified` — [Tools](../en/reference/tools/)
  - Re-verified and migrated to complete EN/RU routes on 2026-08-02 against all thirteen built-in baker registrations, tool application entry points, Mapper/viewer/particle-authoring owners, focused tests, package roles, and tool/build routing docs. The current reference also pins render-only and deferred full-window Mapper capture while the durable flat path preserves every former H2/H3 route.
- `verified` - [Mapper Tools](../en/how-to/tools/mapper.md)
  - Verified against `Source/Applications/MapperApp.cpp`, `Source/Tools/Mapper.*`, mapper/common script methods, client map-view helpers, geometry transforms, and the current embedding-project mapper-render/map-preview scripts on 2026-05-18.
- `verified` - [Animation and Particle Viewers](../en/how-to/tools/animation-particle-viewers.md)
  - Re-verified and migrated to complete EN/RU routes on 2026-08-02 against both focused viewer implementations and application hosts, Mapper embedding, CMake source/library/application/output wiring, package roles, data-source mounting, per-user settings, and the Engine-owned MinimalMultiplayer target layout.
  - Windows `RelWithDebInfo` builds of `FOMM_AnimationViewer` and `FOMM_ParticleViewer` completed cleanly. Both applications remained alive through an eight-second DirectX startup smoke with the expected config and zero error/fatal/assertion/exception lines.
  - The guide separates source/bake validation, focused inspection, Mapper placement, representative client behavior, and retained provenance; records every current control and persisted setting; and defines exact evidence limits without inventing a headless or scripted viewer API. `Docs/ViewerTools.md` preserves every former H2/H3 route, eight focused tests plus a mandatory workflow guard pin the contract, and reviewed mirror coverage is 165/195.

## Essentials, infrastructure, and tests

- `verified` — [Essentials.md](../Essentials.md)
  - Created and verified against `Source/Essentials/*.h`, `Source/Essentials/*.cpp`, `BuildTools/cmake/stages/EngineSources.cmake`, and essentials tests on 2026-05-18.
- `verified` — [Configuration and Data Sources](../en/reference/settings/configuration-and-data-sources.md)
  - Re-verified and migrated to complete EN/RU routes on 2026-08-02 against `ConfigFile.*`, `Settings.*`, `DataSource.*`, `FileSystem.*`, `CacheStorage.*`, `SettingsStorage.*`, installed-client writable overlays, baker/package consumers, and focused tests. The audit pins Engine defaults before project input, single-pass command-line precedence, refreshable mounts, best-effort UI preferences, and every former H2 route through the durable flat pointer.
- `verified` — [Testing.md](../Testing.md)
  - Created and verified against `Source/Applications/TestingApp.cpp`, all 79 current `Source/Tests/Test_*.cpp` files, `FO_TESTS_SOURCE`, generated test/coverage target wiring, and `Source/Tests/README.md` on 2026-05-18.
- `verified` - [Profiling.md](../Profiling.md)
  - Verified on 2026-07-31 against the four profile configurations and Tracy
    compile definitions, vendored `0.13.1` client/version boundary, native and
    AngelScript zones, visible/headless frame marks, client/server plots,
    messages, thread naming, allocation instrumentation, and the
    engine-owned minimal multiplayer target/output layout.
  - The guide promotes isolated client/server captures, exact matching capture
    tools, quiet deterministic workloads, retained provenance, repeated
    attempts, and stable self-time export while keeping scenes, runners,
    thresholds, and reports project-owned. `test_docs_profiling.py` pins the
    source and routing contract.
- `verified` — [Documentation maintenance](../en/contributing/documentation/)
  - Created and verified against `../AGENTS.md`, the docs hub, backlog, expansion plan, research template, verification report, and current source-grounded doc conventions on 2026-05-18.

## Native conventions and safety contracts

Docs added after the initial 2026-05-18 backlog slice, covering native C++ vocabulary and safety contracts referenced from `AGENTS.md`:

- `verified` — [SmartPointers.md](../SmartPointers.md)
  - Verified against `Source/Essentials/SmartPointers.h` and the embedding-project audit tooling reference (`Tools/SmartPointerAudit/smart_pointer_audit.py`) on 2026-07-08.
- `verified` — [ExceptionSafety.md](../ExceptionSafety.md)
  - Verified against `Source/Essentials/MemorySystem.h`, `ExceptionHandling.h`, `Containers.h`, `CommonHelpers.h`, `BasicCore.h`, `Source/Server/DataBase.cpp`, `Source/Server/WorkerPool.cpp`, and the pinning tests `Source/Tests/Test_EntityLifecycle.cpp` / `Test_ServerMapOperations.cpp` on 2026-07-08.
- `verified` — [ThreadSafetyAnalysis.md](../ThreadSafetyAnalysis.md)
  - Verified against `Source/Essentials/Threading.h` `FO_TSA_*` annotations and the `-Werror=thread-safety` toolchain enforcement in `BuildTools/cmake/stages/Init.cmake` on 2026-07-08.

## Production documentation program

- `verified` — [ProductionDocumentationPlan.md](../ProductionDocumentationPlan.md)
  - Baseline audit completed on 2026-07-10 against the standalone engine docs/source/build/test surfaces, the Last Frontier documentation corpus, and the public TLA integration project.
  - The plan defines the next program: standalone independence, public-contract generation, first-run tutorials, public example repositories, professional publication, AI-readable outputs, and a final English/Russian mirror.
  - `verified` describes the source-grounded plan document, not completion of its execution phases.
- `verified` — documentation ownership manifest and standalone validation
  - [documentation-manifest.json](../documentation-manifest.json) classifies every maintained Markdown entry by stable ID, audience, Diataxis kind, visibility, translation scope, owner, lifecycle state, destination, and engine-owned source paths.
  - `BuildTools/docs_validate.py`, its focused unit tests, and the `Validate documentation` GitHub Actions job reject unclassified pages, missing sources, broken local links/anchors, and links that resolve outside the engine root.
  - The canonical [Web guide](../en/how-to/platforms/web-debugging.md), [Android guide](../en/how-to/platforms/android-debugging.md), `MapperTools.md`, `Debugging.md`, the canonical [Client Updater](../en/explanation/runtime/client-updater.md), `Nullability.md`, and test-target guidance are separated from Last Frontier-only paths, tasks, configs, scripts, and binary names; the updater was re-audited and migrated to locale routes on 2026-08-01, and Android plus Web followed with complete source/evidence audits and locale migrations on 2026-08-02.
- `verified` — generated source inventory and documentation decisions
  - `BuildTools/docs_inventory.py` deterministically records exported script methods, native test files, and fixed/variable settings in `generated/source-inventory.json`; unit tests and CI reject drift without duplicating current counts in prose.
  - ADR 0001 records the GitHub Pages/Jekyll/Markdown/`fonline.ru` publication route and future `Docs/en` / `Docs/ru` mirrors. ADR 0002 records public API stability and breaking-change policy.
- `verified` - canonical native-codegen API model
  - [generated/api.json](../generated/api.json) is emitted from the existing typed `codegen.py` parse result rather than a second C++ signature parser. It carries family/symbol IDs, overload identity, runtime sides, signatures, defaults, nullability, mutability, command-line redaction-policy state, descriptions, stability, and source provenance.
  - `BuildTools/tests/test_docs_api.py`, `BuildTools/docs_api.py --check`, manifest freshness validation, and the fast documentation CI job reject parser/model drift.
  - The native model defaults unclassified symbols to `internal` and explicitly excludes remote calls, CMake/CLI/package/helper-CLI surfaces, and native-extension ABI details. Each excluded surface now has a separate owning model or project-baked catalog rather than being folded into native symbols.
- `verified` - generated native-codegen Markdown reference
  - [generated/api/index.md](../generated/api/index.md) routes to seven deterministic GitHub Pages-compatible pages for all 2,472 current model symbols: methods, properties, events, types and members, settings, and migration rules.
  - `BuildTools/docs_reference.py` reads only `generated/api.json`, emits stable symbol anchors and source links, and preserves missing descriptions as a visible metadata backlog rather than inventing prose.
  - Focused unit tests, manifest classification, byte-for-byte standalone validation, and the fast documentation CI job reject missing or manually edited generated pages.
- `verified` - source-owned API contract metadata
  - `BuildTools/codegen.py` now parses docs-only `///@ ApiContract` declarations with exact/family selectors, ADR stability labels, lifecycle fields, repeatable examples, notes, and source provenance without adding them to the runtime compatibility hash.
  - API schema v2 rejects unknown/overlapping selectors, missing deprecated replacements, self-replacements, invalid local examples, and incomplete lifecycle fields; focused tests cover overload families and hash invariance.
  - [generated/api/index.md](../generated/api/index.md) reports 1 explicit contract declaration/symbol and 2,466 default-internal symbols. The explicit entry confirms `Game.BreakIntoDebugger` as internal; no stable API promise was guessed.
- `verified` - multi-domain generated contract diff and disposition gate
  - `BuildTools/docs_contract_diff.py` compares native API, CMake, the main BuildTools CLI, package, helper CLI, native-extension, prototype-format, map-format, model-format, text-format, effect-format, image-format, particle-format, font-format, audio, video, and GUI runtime models by stable ID in one deterministic JSON/Markdown report. `BuildTools/docs_api_diff.py` remains the native symbol/overload layer.
  - Baseline `stable`, `experimental`, and `deprecated` breaks plus model-source/scope/contract changes require exact schema-v2 `Docs/contract-change-dispositions.json` entries bound to the affected domain and both domain contract digests. Internal CLI/package/helper-CLI entry changes remain visible without becoming compatibility promises.
  - Focused native and aggregate tests plus standalone manifest tests cover native/CMake/native-extension/prototype-format/map-format/model-format/text-format/effect-format/image-format/particle-format/font-format/audio/video/GUI/AiControl removals, nested documentation, policy changes, internal-vs-experimental enforcement, stability withdrawal, stale dispositions, model drift, partial bootstrap, report artifacts, and missing enforcement. GitHub Actions compares the complete PR/push range and preserves the eighteen-domain report on failure.
- `verified` - project remote-call reference and generated catalog
  - [Remote Calls](../en/reference/scripting/remote-calls.md) owns the declaration, direction, handler, namespace, serialization, authority, compatibility, and validation contract without importing game-specific calls into engine docs.
  - `BuildTools/docs_metadata.py` strictly decodes paired server/client `.fometa` outputs from the owning `MetadataBaker`, rejects malformed or mismatched records, and emits stable project-owned JSON/Markdown IDs without reparsing `.fos`.
  - Four focused tests cover binary framing, pairing, nullability, malformed input, determinism, and write/check behavior. The engine-owned starter proves both side handlers and the decoder against actual baked metadata in `win64-starter-smoke`.
- `verified` - generated CMake project-interface reference
  - `BuildTools/cmake/ProjectInterface.json` is read by configure and the documentation generator, so public option declarations, stage order/entrypoints/hooks, and selected helper names have one versioned source of truth.
  - [generated/cmake/index.md](../generated/cmake/index.md) routes to checked option, stage/hook, and helper pages backed by the canonical [generated/cmake.json](../generated/cmake.json) model and stable `cmake.*` IDs.
  - Five focused Python tests, a structural CMake script, manifest freshness validation, and the fast documentation job reject schema, runtime-command, source-path, escaping, and committed-output drift.
- `verified` - generated BuildTools CLI reference
  - `BuildTools/docs_cli.py` imports the executable `BuildTools/buildtools.py::create_parser()` and emits [generated/cli.json](../generated/cli.json) plus checked index/command pages with stable `cli.buildtools.*` IDs, exact help output, defaults, choices, and cardinality.
  - Four focused tests compare generated help with the executable, cover parser boundaries/determinism/escaping/write-check behavior, and the standalone validator plus fast documentation job reject stale CLI artifacts.
  - The 12-command surface is explicitly `internal` until a versioned support policy is approved; package and helper CLI contracts use their own completed models, and all participate in multi-domain diff enforcement.
- `verified` - generated helper CLI reference
  - `BuildTools/HelperCliInterface.json` owns stable helper identity, purpose, audience, invocation owner, and explicit exclusions; executable `create_parser()` factories remain authoritative for syntax.
  - `BuildTools/docs_helper_cli.py` emits [generated/helper-cli.json](../generated/helper-cli.json) plus checked index/command pages for 7 helpers, 11 commands, and 53 arguments with stable `helper-cli.*` IDs and exact help output.
  - Four focused tests execute every real helper/subcommand help path, enforce complete AST parser inventory, determinism, escaping, and stale detection. Manifest validation, CI freshness checks, and the fifth aggregate diff domain reject drift without promoting this `internal` surface to stable.
- `verified` - native-extension interface and executable conformance
  - `BuildTools/NativeExtensionInterface.json` is consumed by CMake/codegen and owns the five source roles, eight optional engine hooks with generated fallbacks, and six binding rules under stable `native-extension.*` IDs.
  - [NativeExtensions.md](../NativeExtensions.md) documents source composition, roles, exports, hook lifecycle, instance-owned state, dependencies/platform guards, revision compatibility, and validation. [generated/native-extension/index.md](../generated/native-extension/index.md) routes to deterministic role, hook, and binding pages plus the canonical JSON model.
  - Unknown CMake roles now fail at configure time; the structural test proves routing/header behavior and role parity with the project-interface manifest. Five focused generator tests, standalone validation, CI freshness checks, and the sixth aggregate domain reject manifest/runtime/reference drift under an explicit `experimental`, revision-pinned policy.
  - The engine-owned starter compiles a `SERVER` extension, suppresses one hook fallback, generates `Game.NativeStarterValue()`, calls it from baked AngelScript, and requires `starter_native_extension_value=42` at runtime.
- `verified` - project-local dependency ownership and revision-pinned linking
  - [ProjectDependencies.md](../ProjectDependencies.md) owns the reusable Engine/project decision, vendored/imported/system/runtime delivery models, dependency record, controlled `find_package()` boundary, platform contract, ABI/allocation/lifetime rules, package payloads, licensing/security review, update/rollback procedure, and validation matrix.
  - The current Engine has no selected project-library helper or mapper-only list. Embedding projects use revision-pinned `FO_COMMON_LIBS`, `FO_SERVER_LIBS`, `FO_CLIENT_LIBS`, `FO_BAKER_LIBS`, and `FO_TESTING_LIBS` integration state and must re-audit it on Engine updates.
  - The minimal project carries a server-only `INTERFACE` usage requirement that fails compilation when propagation through `FO_SERVER_LIBS` is absent. Structural CMake and focused documentation tests cover the current integration contract.
- `verified` - prototype-format guide and generated reference
  - [Prototype Format](../en/how-to/content/prototype-format.md) owns reusable file selection, section resolution, identity, inheritance, property applicability, strict values, references, migrations, project boundaries, and authoring practices without depending on Last Frontier.
  - `BuildTools/PrototypeFormatInterface.json` and `BuildTools/docs_prototype_format.py` validate live parser/baker anchors, derive 4 built-in `HasProtos` types and 113 properties from current metadata, and emit the canonical model plus four EN reference pages, durable legacy routes, and stable `prototype-format.*` IDs. The current model contains 3 section forms, 2 control directives, and 13 rules.
  - Eight focused tests, manifest freshness validation, documentation CI, and the seventh aggregate contract domain reject grammar/source/property/route drift. Grammar/rules are `experimental`; the revision-derived property inventory stays `internal`.
  - The complete Russian guide and four reference mirrors preserve fenced examples, stable IDs, source links, property inventory, and translation freshness, raising reviewed mirror coverage to 154/195.
  - Last Frontier now routes engine grammar to this guide while retaining project extensions, custom metadata, migration CI, field semantics, and gameplay validation in its own `Docs/Prototypes.md`.
- `verified` - map-format guide and generated reference
  - [Map Format](../en/how-to/content/map-format.md) owns strict `[ProtoMap]` plus `[$Name/Critter]` / `[$Name/Item]` placement syntax, map and placement identity, ownership references, property overrides, mapper normalization, side-specific baking, bounds, static/dynamic behavior, and runtime materialization without importing Last Frontier catalogs or composition policy.
  - `BuildTools/MapFormatInterface.json` and `BuildTools/docs_map_format.py` validate live loader/baker/mapper/runtime anchors, derive 108 current Map/Critter/Item properties and all 4 `ItemOwnership` values from metadata, and emit the canonical model plus five reference pages with stable `map-format.*` IDs.
  - Focused tests, manifest freshness validation, documentation CI, and the eighth aggregate domain reject source/model/reference drift. Grammar/ownership/rules are `experimental`; the revision-derived property inventory stays `internal`.
  - Last Frontier routes reusable format mechanics to the engine guide while retaining its piece catalog, visual grammar, AI map-authoring tools, project metadata, and semantic validation in `Docs/MapAuthoring.md`.
- `verified` - model-format and 3D composition guide with generated reference
  - [Model Format](../en/how-to/content/model-format.md) owns current `.fo3d` lexical syntax, include templates, parser state, FBX/OBJ inputs, compile-time limits, layers, root modifiers, child models, particles, transforms, textures/effects, disables, cuts, rendering flags, runtime composition, failures, practices, and project boundaries.
  - `BuildTools/ModelFormatInterface.json` and `BuildTools/docs_model_format.py` compare 59 accepted parser spellings grouped into 32 stable token entries directly with `ModelDescriptionParser::ParseToken`, validate live baker/runtime/test anchors and `FO_MODEL_*` options, and emit the canonical model plus seven EN reference pages and durable legacy routes.
  - Nine focused tests, manifest freshness validation, documentation CI, and the ninth aggregate contract domain reject parser/manifest/source/reference drift. The guide explicitly rejects removed legacy tokens and `.x` / `.3ds` inputs instead of copying historical project examples.
  - Last Frontier retains concrete model assets, layer meanings, animation enums, art policy, gameplay timing, and visible project scenes while routing reusable format mechanics to the Engine guide.
- `verified` - text and localization guide with generated reference
  - [Text and Localization](../en/how-to/content/text-and-localization.md) owns raw `.fotxt` syntax, structured keys, multiline values, comments, variants, ordered language normalization, prototype `$Text`, script lookup, language switching, renderer color tags, diagnostics, authoring practices, and the project-formatting boundary.
  - `BuildTools/TextFormatInterface.json` and `BuildTools/docs_text_format.py` validate live parser/baker/runtime/settings/renderer anchors, derive Engine language defaults and all five prototype output packs, and emit the canonical model plus six reference pages with 38 stable `text-format.*` entries.
  - Seven focused tests, manifest freshness validation, documentation CI, and the tenth aggregate contract domain reject source/model/reference drift. The reusable contract is `experimental`; project pack catalogs, language priorities, translation workflow, and lexem formatters remain project-owned.
  - Last Frontier routes parser, baking, and runtime mechanics to the Engine guide while retaining Russian-first policy, concrete packs, translation guards, semantic key conventions, `TextFormatting.fos`, and GUI refresh behavior in `Docs/Localization.md`.
- `verified` - effect-format guide with generated reference
  - [Effect Format](../en/how-to/content/effect-format.md) owns `.fofx` sections, shader-stage fallback, passes and render state, vertex layouts, built-in resources, backend artifacts, runtime cache identity, script values, diagnostics, authoring practices, and the embedding-project boundary.
  - `BuildTools/EffectFormatInterface.json` and `BuildTools/docs_effect_format.py` validate live baker, renderer, runtime, script-method, settings, and focused-test anchors; derive current effect limits; and emit the canonical model plus seven reference pages with stable `effect-format.*` entries.
  - Seven focused tests, manifest freshness validation, documentation CI, and the eleventh aggregate contract domain reject source/model/reference drift. The reusable contract is `experimental`; project shader catalogs, visual policy, effect-slot selection, and `ScriptValueBuf` meanings remain project-owned.
  - Last Frontier routes reusable mechanics to the Engine guide while retaining `Resources/Visual/Effects/`, minimal/advanced profile policy, slot-specific tuning, and visible project validation in `Docs/Scripts.md` and `Docs/ContentWorkflow.md`.
- `verified` - image-format and FOFRM guide with generated reference
  - [Image And Sprite Formats](../en/how-to/content/image-format.md) owns all 12 built-in image source extensions, source selection, FOFRM fields/aliases/directions/flattening/timing, ART/SPR/BAM selectors, pixel/alpha constraints, the private baked container, stock runtime factory coverage, sprite sheets, atlases, caches, diagnostics, authoring practices, and the embedding-project boundary.
  - `BuildTools/ImageFormatInterface.json` and `BuildTools/docs_image_format.py` validate live baker/client/test anchors, derive the 12 baker and 11 default-runtime extensions, prove the direct-SPR and ignored-Effect boundaries, and emit the canonical model plus seven reference pages with 49 stable `image-format.*` entries.
  - Seven focused tests, manifest freshness validation, documentation CI, and the twelfth aggregate contract domain reject source/model/reference drift. The reusable contract is `experimental`; private byte-record entries are `internal`, the parsed-but-unused Effect field is `deprecated`, and project assets/licensing/pack policy/visual acceptance remain project-owned.
  - Last Frontier routes reusable mechanics to the Engine guide while retaining its PNG/TGA/FOFRM catalog, source/provenance policy, resource-pack precedence, substitutions, movement tuning, hit-test expectations, and visible client baselines in `Docs/ContentWorkflow.md`.
- `verified` - SPARK/Effekseer particle formats, tooling, baking, and runtime guide with generated reference
  - [Particle Format And Runtime](../en/how-to/content/particle-format.md) owns independent backend selection; `.spark`/`.spk` and `.efkproj`/`.efk` delivery; SPARK graph/editor rules; the supported Effekseer compiler/render profile; mandatory baked bounds; atlas/direct-scene rendering; script/model integration; diagnostics; authoring practices; and embedding-project validation.
  - `BuildTools/ParticleFormatInterface.json` and `BuildTools/docs_particle_format.py` derive two backends, four authored/runtime formats, 37 SPARK graph objects, 19 renderer/capability records, tool/runtime/integration rules, Engine defaults, and source behavior into a canonical model plus eight reference pages with 113 stable `particle-format.*` entries.
  - Eleven focused tests, manifest freshness validation, documentation CI, and the thirteenth aggregate contract domain reject registry/editor/source/model/reference/legacy-route drift. The reusable surface is `experimental`; derived implementation coverage stays `internal`, and project catalogs, effects, textures, settings overrides, budgets, art direction, and visual acceptance remain project-owned.
  - Last Frontier routes reusable mechanics to the Engine guide while retaining its seven particle systems, six textures, concrete effects, scene/shot/grenade integrations, render overrides, and visible project gates in `Docs/ContentWorkflow.md`.
- `verified` - interactive Mapper and particle-authoring manuals with versioned screenshots
  - [Mapper Interactive Manual](../en/how-to/tools/mapper-interactive.md) owns the stock menu/window map, editing,
    selection, Inspector, history, save discipline, settings recovery,
    troubleshooting, and the map-only versus full-window capture boundary.
  - [Particle Authoring Tools](../en/how-to/tools/particle-authoring.md) owns Particle
    Preview, the SPARK source browser/editor, pinned Effekseer authoring,
    Particle Viewer routing, a minimal source fixture, and visible validation.
  - `Examples/MinimalMultiplayer` supplies the independent looping SPARK
    source, MIT-provenance texture, deterministic Mapper profile, and deferred
    capture script. Windows Release baking plus `FOMM_Mapper` and
    `FOMM_ParticleViewer` builds completed cleanly on 2026-07-31.
  - `BuildTools/DocumentationScreenshots.json`,
    `BuildTools/docs_screenshots.py`, focused tests, manifest freshness, and
    site image decoding pin two 1280x800 Direct3D captures to exact image,
    source, environment, interaction, and recapture metadata.
  - Source review replaced the SPARK editor's stale private baked-sprite parser
    with the canonical versioned `SpriteResource` reader and added a deferred
    pre-present full-window Mapper screenshot API while retaining the existing
    synchronous map-only API.
- `verified` - bitmap-font format, binding, layout, and rendering guide with generated reference
  - [Font Formats And Text Layout](../en/how-to/content/font-format.md) owns FOFNT and AngelCode BMFont descriptors, raw-copy and referenced-image delivery, slot binding, bind-time downscale, metrics, wrapping/alignment, draw flags, inline colors, atlas/cache behavior, diagnostics, authoring practices, and the embedding-project boundary.
  - `BuildTools/FontFormatInterface.json` and `BuildTools/docs_font_format.py` derive the live extensions, FOFNT keys/version, BMF v3 block and signed-metric contract, font/flag/layout enums, scale guard, atlas/cache behavior, and bundled descriptor evidence into a canonical model plus eight reference pages with 57 stable `font-format.*` entries.
  - Nine focused tests, manifest freshness validation, documentation CI, and the fourteenth aggregate contract domain reject descriptor/source/model/reference drift. The reusable surface is `experimental`; cache internals stay `internal`, and project slot assignment, glyph coverage, typography, localization acceptance, and UI baselines remain project-owned.
  - Source review fixed BMFont negative `xoffset`, `yoffset`, and `xadvance` decoding to use signed 16-bit reads and rejects an empty FOFNT image name before path access. Last Frontier routes reusable mechanics to the Engine guide while retaining concrete fonts, Russian glyph coverage, GUI slot policy, and embedded-client measurement/visual gates.
- `verified` - audio delivery, decoding, playback, and mixing guide with generated reference
  - [Audio](../en/how-to/content/audio.md) owns WAV/ACM/Ogg delivery, narrow decoder contracts, normalized effect identities, contiguous numbered variants, exact-path music, repeat timing, SDL conversion/mixing, silent/headless behavior, diagnostics, authoring practices, and the embedding-project boundary.
  - `BuildTools/AudioInterface.json` and `BuildTools/docs_audio.py` derive three formats and 32 stable `audio.*` entries from resource indexing, decoder, raw-copy, script API, frontend, settings, headless, and native-test sources, then emit a canonical model plus six reference pages.
  - Nine focused tests, manifest freshness validation, documentation CI, and the fifteenth aggregate contract domain reject source/model/reference drift. The reusable surface is `experimental`; project music state, ambient and recipient policy, mastering, asset licensing, and audible acceptance remain project-owned.
  - Source review now rejects an explicit unsupported sound suffix before enqueueing an undecoded object. No focused native `SoundManager` codec/playback fixture exists yet, so raw-copy/native regression coverage and source-backed checks do not replace a visible audible client pass.
  - Last Frontier routes reusable mechanics to the Engine guide while retaining its sound pack, music/combat/death transitions, ambient emitters, multiplayer recipients, literal resource integrations, volume UI, provenance obligations, and visible validation in `Docs/Audio.md`; the Engine owner is [Audio](../en/how-to/content/audio.md).
- `verified` - experimental video delivery, decoding, fullscreen, and embedded playback guide with generated reference
  - [Video](../en/how-to/content/video.md) owns exact-path OGV raw-copy delivery, Ogg/Theora headers and frames, whole-resource memory, CPU RGBA conversion, fullscreen replacement/queue/input/music/draw order, embedded `RenderIface` playback, diagnostics, loop risk, and the embedding-project boundary.
  - `BuildTools/VideoInterface.json` and `BuildTools/docs_video.py` derive 34 stable `video.*` entries from decoder, client, script, renderer, settings, baker, dependency, and native-test sources, then emit a canonical model plus seven reference pages.
  - Nine focused tests, manifest freshness validation, documentation CI, and the sixteenth aggregate contract domain reject source/model/reference drift. The complete surface is `experimental`; no focused native video fixture or production loop proof exists, so visible asset/platform acceptance remains mandatory.
  - Last Frontier accurately records its current no-video state and owns a staged production adoption plan for a dedicated pack, catalog, server authority and recipients, controller, skip/state policy, Russian/English subtitles, audio restoration, provenance, budgets, and visible validation in its `Docs/Video.md`; the Engine owner is [Video](../en/how-to/content/video.md).
- `verified` - reusable GUI runtime guide with generated reference
  - `BuildTools/GuiRuntimeInterface.json` and `BuildTools/docs_gui_runtime.py` derive 12 runtime types, 159 documented members, 39 callbacks, 31 screen API overloads, 8 metadata annotations, and 82 stable `gui-runtime.*` entries from `Gui.fos`, `Input.fos`, native client dispatch, tutorial boundaries, and test inventory.
  - Focused tests, manifest freshness validation, documentation CI, and the seventeenth aggregate contract domain reject type/member/callback/screen API/annotation/lifecycle/layout/input/integration drift. The full surface remains `experimental` because no self-contained native GUI runtime fixture or standalone client example proves pixels and interaction.
  - Engine explicitly does not parse `.fogui` or `.foguischeme`. Last Frontier retains its declarative format, generator, editor, generated catalog, concrete screens/styles/assets, project lifecycle bridge, and visible platform/language/accessibility gates in `Docs/GuiSystem.md`.
  - TLA's separate Video pack and remote wrappers remain historical integration evidence only; the audited intro call is disabled by a framebuffer/empty-texture regression note and is not normative proof.
- `verified` - project-neutral AiControl protocol, reference client, and runnable sample
  - [AiControl Protocol](../en/how-to/ai-control-protocol.md) owns only the UTF-8 NDJSON/TCP envelope, six methods, common errors and command fields, bounded queue/event lifecycle, `command_completed` correlation, loopback threat boundary, native-extension integration rules, MCP ownership boundary, and evidence limits.
  - `BuildTools/AiControlProtocol.json` and `BuildTools/docs_ai_control_protocol.py` derive 49 stable `ai-control-protocol.*` entries from the standard-library client and standalone sample, then emit one canonical model plus six generated reference pages as the eighteenth aggregate contract domain.
  - `Examples/AiControlSample` passes a 12-check end-to-end smoke. Eight focused malformed-peer/security/state tests, five documentation tests, helper-CLI discovery, manifest freshness, aggregate diff, and CI routing reject protocol and documentation drift.
  - Last Frontier and TLA native bridges, scripts, docs, and MCP adapters were audited as convergence evidence. Their observation fields, game actions, administrator tools, readiness semantics, launch orchestration, and `lf_*`/`tla_*` namespaces remain project-owned. The Python sample is protocol proof, not native FOnline runtime or server-authority proof.
- `verified` - model animation metadata and duration guide
  - [ModelAnimation.md](../ModelAnimation.md) owns `.fo3d` animation tuples, positive authored speed, effective cycle calculation, one-step state/action aliases, `ModelAnimationInfo.foinfo`, common and client-instance lookup boundaries, failures, authoring practices, and project ownership without depending on Last Frontier or TLA.
  - Five focused source tests pin the guide to `ModelInfoBaker`, client alias selection, common metadata registration, both script exports, and native model/common-script regressions. The standalone validator requires the focused test in documentation CI.
  - [Model Format](../en/how-to/content/model-format.md) now owns the surrounding `.fo3d` grammar and runtime composition; Last Frontier retains concrete model assets, resource packs, enum selection, gameplay timing, fallback behavior, and project tests.
- `verified` - 2D sprite root motion and walk-cycle guide
  - [Sprite Root Motion](../en/how-to/content/sprite-root-motion.md) owns `NextX` / `NextY` import and baked transport, direction-specific `SpriteSheet` offsets, authoritative-movement separation, anchor-relative phase, frame selection, rendered alignment, sheet changes, lifecycle, fallbacks, and project validation.
  - Five focused source tests pin the guide to current `ImageBaker`, `DefaultSpriteFactory`, `MovingContext`, `CritterHexView`, image-baker regressions, manifest routing, and CI. The guide explicitly records the missing focused native `CritterHexView` fixture and requires a visible client scene for semantic gait validation.
  - The reusable material discovered in TLA `Docs/Animation.md` was re-derived against current Engine source. TLA remains integration evidence, not a normative dependency; its 2D sprite contract is kept separate from `.fo3d` skeletal animation.
- `verified` - script lifecycle and concurrency guide
  - [Script Lifecycle and Concurrency](../en/how-to/scripting/lifecycle-and-concurrency.md) now owns module-init ordering and global freeze, callback-only attributes and entity lifetime, transitive `[[Async]]`, client/server `Yield` resumption, server entity covers, `Game.Lock`, mutable-state ownership, and shutdown boundaries independently of an embedding project.
  - [Scripting](../en/explanation/scripting-runtime/), human/AI indexes, test routing, and the translation manifest route readers to the focused guide instead of mixing project module topology with reusable runtime semantics.
  - `BuildTools/tests/test_docs_script_lifecycle.py` pins the guide's central claims to current `ScriptSystem`, AngelScript attributes/context, client scheduling, server synchronization, and entity teardown markers; the standalone validator requires this test in documentation CI.
- `verified` - generated package declaration and payload reference
  - `BuildTools/PackageInterface.json` is consumed by `package.py` and `BuildTools/docs_package.py`, producing [generated/package.json](../generated/package.json) plus five checked pages for `DefinePackage`, targets/platforms/packs, payloads/artifacts, and the internal packager CLI.
  - The model exposes stable `package.*` IDs for 6 targets, including the focused `AnimationViewer` and `ParticleViewer`, 6 platforms, 19 packs, 6 payloads, and 13 CLI arguments while marking macOS/iOS unsupported and AppImage placeholder instead of implying support.
  - Five focused Python tests, a structural CMake test of the real declaration macro, runtime combination validation, manifest freshness checks, and documentation CI reject schema/parser/CMake/output drift.
- `verified` — GitHub Pages-compatible preview contract
  - [Documentation site publication](../en/contributing/documentation/site-publication.md) records the Markdown/Jekyll/custom-domain contract, exact local runtime pins, preview commands, DNS verification, CI artifact review, and the boundary between repository data and private domain credentials.
  - The `Build documentation site` job uses the official Pages Jekyll build action, validates the exact `_site` tree statically and in lock-file-pinned Chromium, and uploads commit-addressable site/report/screenshot artifacts for 14 days without changing the existing production deployment route.
  - Manifest validation rejects drift between `CNAME`, `_config.yml`, `.ruby-version`, `Gemfile`, Node/Playwright/axe pins, the Pages source-verification state, and the build/upload workflow. The post-build validator checks every manifest route, locale index, static endpoint, canonical URL, local reference, and baseline accessibility invariant. The browser validator checks every route at 1440 x 1000 and 390 x 844 for WCAG 2.2 A/AA axe violations, runtime/resources, containment/overflow, and focused keyboard interactions. Clipped `color-contrast` incomplete nodes are resolved from computed effective colors and WCAG luminance or fail the route. The local production-compatible artifact passes 421 rendered routes, 43 static endpoints, 56,215 references, and all 842 browser page/profile checks plus eleven interactions and thirteen screenshots with zero violations/errors; all 8,509 encountered raw incomplete nodes pass the reviewed fallback with zero failed/unresolved results. The first landed CI artifact, manual assistive-technology/zoom review, and administrator confirmation of the production source branch/folder remain pending.
- `verified` — canonical minimal project, Windows and Linux paths
  - [../Examples/MinimalProject/README.md](../../Examples/MinimalProject/README.md) now owns a complete headless scaffold with CMake composition, explicit minimal configuration, AngelScript lifecycle, a server native hook/export, deterministic smoke sub-config, and a timeout/marker runner.
  - `python BuildTools/buildtools.py validate win64-starter-smoke` passed on 2026-07-10 after recreating the disposable project copy, baking scripts, reaching both lifecycle markers, and shutting down cleanly.
  - Private template run `29739863448` passed the exact-pin primary smoke on clean `windows-latest` and `ubuntu-latest` runners on 2026-07-20; manual current-Engine run `29740066760` passed the same Linux route after checking out Engine `master`. Linux host dependencies come from the checked-out Engine's versioned preparation command.
- `verified` — first tested tutorial lesson
  - [First FOnline Headless Project](../en/tutorials/first-project.md) documents the tested headless milestone, expected output and artifacts, a rebake exercise, deliberate starter limits, and recovery from the integration failures found while building it; [../TUTORIAL.md](../../TUTORIAL.md) remains its durable legacy route.
- `verified` - first playable client, content, and test lessons
  - [../Examples/MinimalMultiplayer/README.md](../../Examples/MinimalMultiplayer/README.md) owns a standalone desktop/headless client, headless server, baker, one location/map, player, guide NPC, item interaction, paired remote calls, synchronized persistent property, and English/Russian prototype text.
  - [First Playable Client](../en/tutorials/first-client.md), [First Content Change](../en/tutorials/first-content.md), and [First Automated Test](../en/tutorials/first-test.md) teach the same executable source through connection/map load, a localized content change, metadata/content/client-visible test layers, expected evidence, and recovery.
  - `windows-check` and `python BuildTools/buildtools.py validate win64-tutorial-smoke` pass the complete source route. The generated complete config and `windows-package` lane also pass raw/archive parity, SHA-256 inventory, packaged updater/gameplay interaction, and clean shutdown. Linux smoke/package targets are wired into CI but still need clean-runner evidence before a cross-platform publication claim.
  - Runtime defaults are applied before project overrides and protected by the focused Settings test. Vendored AngelScript CMake detects MASM through the compiler variables used by current CMake/Visual Studio and has a focused structural regression.

## Next recommended slice

- `verified` - evidence-scoped support matrix and project integration workflows
  - The [Support Matrix](../en/reference/platforms/support-matrix.md), `BuildTools/SupportMatrix.json`, and the generated JSON/Markdown projection distinguish build-gated, process-smoke-gated, source-capable, unsupported, and embedding-project/device qualification for ten current profiles.
  - [Configure a Game Project](../en/how-to/build/project-configuration.md) owns `.fomain`, resource-pack descriptors, exact precedence, sub-config inheritance, environment/file substitution, baking, validation, and recovery without depending on Last Frontier.
  - [Generated Content Workflow](../en/how-to/build/generated-content.md) owns configure-time generation, scripts, baked resources, side metadata, documentation models, site/locale/AI artifacts, dependency order, and stale-output recovery.
  - The [Engine Upgrade Guide](../en/how-to/migration/engine-upgrade.md) owns complete-range update review, generated-contract comparison, configuration/content reconciliation, save backup/restore, gameplay/updater/ABI compatibility, rollout, validation, and same-change documentation.

- `verified` - localization freshness infrastructure
  - `Docs/translation-glossary.json` owns the first 34 stable English/Russian terminology records; `BuildTools/docs_localization.py` emits [generated/translation-status.json](../generated/translation-status.json) and validates normalized source hashes, exact fenced code, and language-preserving links for every existing Russian counterpart.
  - Focused tests, standalone validation, and documentation CI reject malformed glossary records, wrong document IDs/paths, stale hashes, code drift, and cross-locale link drift.
  - Physical localization is complete: all 197 required counterparts are current and CI requires `--enforce-complete`. The generated-description catalog also runs in `complete` mode: all 4,918 reader-facing values across twenty machine-model domains, including 2,474 native API descriptions and contract notes, have current reviewed records. All 2,472 Native API symbols now have descriptions and Russian overlays: 121 events, 133 properties, every method including all 236 common `ImGui` exports, 22 value types and their 62 layout fields, 28 migration rules, seven entities, 54 enum types, all 746 enum values, and six reference types with their 41 fields and 28 methods. The enum-value total comprises 140 generated property identifiers, 159 source-authored ordinary values, 302 Dear ImGui values, 40 opaque information-message slots, and 105 key codes. Exact-source translation memory safely reuses the 133 owning-property translations, and the same property prose is also localized at all 221 generated map/prototype-format locators. Missing, stale, unknown, ambiguous-reuse, type-changing, list-shape-changing, or inline-code-changing entries fail CI without conflating semantic coverage with route parity.

- `verified` - manifest-backed AI documentation delivery
  - `BuildTools/docs_ai_delivery.py` generates root `llms.txt`, 2 MiB-bounded `llms-full.txt`, and public `docs-manifest.json` from the same source manifest and Markdown used by the human site. Every public record exposes source-ref-pinned clean Markdown plus canonical human HTML; fail-closed validation and the no-truncation rule remain unchanged.
  - Focused tests and standalone validation enforce public/current filtering, generated-index-only context policy, canonical/source/Markdown/raw URLs, normalized SHA-256 hashes, deterministic output, workflow wiring, and the byte budget. ADR 0003 records the ownership and non-normative delivery contract.
- `verified` - versioned AI documentation evaluation
  - [AI Documentation Evaluation](../en/contributing/documentation/ai-evaluation.md) and `Docs/ai-evaluation.json` own 27 representative tasks across architecture, scripting, content, debugging, migration, and release, with 65 retrieval checks and 92 answer checks. The set covers AngelScript style/refactoring, gameplay-test boundary selection/process semantics, project-local dependency ownership/link/package boundaries, interactive and automated Mapper workflows, package, secret, server-operations, and backup/recovery procedures.
  - `BuildTools/docs_ai_eval.py` executes the same ranking contract as browser search and emits [generated/ai-evaluation-report.json](../generated/ai-evaluation-report.json). Focused tests, standalone validation, CI, and the rendered-site artifact gate reject stale tasks, owners, headings, evidence, ranks, thresholds, or output.
  - The first deterministic run exposed repeated-prefix and absent-token bias; Python and browser ranking now count each effective query token once. The source-owned threshold passes all 65 checks at 100 percent with 0.908 MRR. The final 2026-08-04 Qwen 3.5 and GPT-OSS qualifications each selected 27/27 owners and received all 92 rubric criteria in their isolated inputs. Independent review accepted 27/27 Qwen tasks and 25/27 GPT-OSS tasks, so both exceed the unchanged 90-percent semantic production target.
  - The completed remediation adds bilingual decision/checklist summaries to twenty-one owning pages and generated entry points. The optional Ollama harness repeats bounded, verbatim decision and query-relevant sections without exposing hidden answer checks, retains one invalid-response retry and one objective semantic-completion repair, and records all attempts with hidden reasoning redacted. The two remaining GPT-OSS answer failures are explicit in the compact review; neither is an unsupported safety, migration, compatibility, or release claim. Historical pre-remediation baselines remain immutable.
- `verified` - complete fenced-snippet parser coverage
  - [Documentation Snippet Validation](../en/contributing/documentation/snippets.md) and `BuildTools/SnippetPolicy.json` define the public/current/human fence scope, eleven supported languages, normative/evidence distinction, template reporting, and semantic-owner boundary. XML examples use a real `ElementTree` parser rather than being mislabeled as text.
  - `BuildTools/docs_snippets.py` inventories every scoped fence in [generated/snippets.json](../generated/snippets.json), rejects untyped/unknown/unclosed blocks, and requires 100 percent of normative commands, source fragments, configuration, and data to pass the declared harness.
  - Bash and PowerShell blocks also pass their real language parsers in a separate no-execution CI job. Focused tests and standalone validation reject malformed source, policy drift, stale output, missing workflow wiring, or an attempted ignore path.
- `verified` - source-owned visual teaching diagrams
  - `BuildTools/DocumentationDiagrams.json` owns the Engine/game boundary, generated-content dependency flow, and human/AI documentation delivery diagrams. Each record binds one owning document, descriptive alt text, explanatory caption, canvas geometry, and the Engine source paths that justify the model.
  - `BuildTools/docs_diagrams.py` emits six local accessible SVG variants for three diagrams under `Docs/assets/diagrams/` plus [generated/diagrams.json](../generated/diagrams.json) with exact source-manifest and SVG hashes. Mobile variants use a generated vertical reading order and connection list rather than shrinking desktop text. The tool rejects missing provenance, invalid roles/IDs, canvas overflow, node overlap, absent owning markup, external resources, scripts, and stale output.
  - Focused tests, aggregate manifest validation, static site endpoint checks,
    decoded-image checks on every rendered route, retained desktop/mobile/200-percent
    architecture screenshots, and the versioned Mapper/SPARK screenshot
    catalog keep the visual layer reproducible. Additional runtime screenshots
    remain task-driven rather than a prerequisite for documenting unrelated
    systems.
- `verified` - manifest-backed documentation site navigation and search
  - `BuildTools/docs_site.py` generates `_data/docs-site.json` for locale-aware Jekyll navigation plus `assets/docs-search.json` and `assets/docs-search.ru.json` for browser-local search from the same stable document IDs and Markdown corpus.
  - Navigation covers every public current human top-level page exactly once while generated detail pages remain searchable behind their indexes. Each locale artifact has its own reviewed hard 1.75 MiB budget, raised explicitly when the 163-page Russian mirror exceeded the former 1.25 MiB ceiling; Russian results contain only current Russian routes, and generation still fails instead of dropping documents.
  - The custom default layout provides responsive desktop/mobile navigation, rolling `master` identity, stable-ID language switching, localized labels, page-local table of contents, source links, code-copy controls, and persisted light/dark preference without a remote application or asset dependency.
  - Focused generator and layout/static tests, standalone freshness validation, and CI wiring reject missing/duplicate navigation IDs, stale/oversized locale indexes, cross-locale results, missing local assets, and rendering-contract drift. ADR 0004 owns the boundary.

- `verified` - rendered GitHub Pages artifact gate
  - `BuildTools/docs_site_artifact.py` validates the actual Jekyll `_site` output against the manifest, route catalog, search model, and generated static endpoints, then preserves a machine-readable report in CI.
  - The audit covers canonical URLs, locale metadata, one accessible page landmark/title/H1, skip links, image alt text, button names, duplicate IDs, local links/fragments/resources, every locale search target, and byte-identical public JSON/text artifacts. The browser interaction gate separately proves Russian search and paired-language navigation.
  - A production-mode build with `github-pages` `232`, Bundler `2.5.11`, and local Ruby `3.3.4` passes 583 rendered routes, 43 public static endpoints, and 81,889 local references; Chromium passes 1,749 desktop/mobile/200-percent-reflow page checks, fourteen interaction profiles, and seventeen screenshots with no failed or unresolved accessibility result. The manifest-owned zoom profile uses a 640 x 512 CSS viewport, device scale factor 2, and a 1280 x 1024 physical Russian screenshot; direct review found the first capture still in the closing-drawer transition, so the harness now waits until the sidebar is fully outside the viewport and the corrected image is readable without overlap or clipping. The artifact gate also rejects any rendered or raw variant of an internal document or generated model. CI remains authoritative for the exact Ruby `3.3.4` pin. Earlier work corrected nested README permalinks, Jekyll directory-index canonicals, a Liquid delimiter collision, line-wrapped Markdown links, unstable generated anchors, nested diagram paths, physical-route assumptions, Markdown/Jekyll slug mismatches, one duplicate translated-heading compatibility anchor, one cross-locale fragment, and nested screenshot-owner paths; the locale slices additionally corrected active-switch contrast and Unicode browser tokenization.

- `verified` - documentation version, locale, and stable route policy
  - [ADR-0006](../en/contributing/decisions/0006-documentation-version-locale-routing.md) defines rolling `master` as the unversioned `current` channel, defers tagged snapshots until supported release lines exist, and records the bilingual route and durable Markdown redirect policy.
  - `BuildTools/docs_site.py` now emits [generated/document-routes.json](../generated/document-routes.json) with every current public URL, canonical future owner, planned English/Russian path, availability state, and required legacy redirect.
  - Focused site, AI-delivery, layout, and standalone-validator tests reject version/source-ref drift, malformed locale pairs, ambiguous canonical targets, route collisions, invalid legacy pointer records, and stale generated route data.
  - All 197 required documents have current English/Russian counterparts under complete-parity enforcement. Every manifest-planned README entrypoint has a paired `README.ru.md`, old flat routes remain durable pointers, and stable-ID language switching plus independent EN/RU search are implemented and gated.

- `verified` - generated cross-domain public contract entry
  - `BuildTools/docs_public_api.py` renders [PUBLIC_API.md](../../PUBLIC_API.md) from all eighteen current machine models and generated human indexes. It preserves each owning stability label and reports the native model's live symbol, description, scope/exact-contract, and unclassified counts without manual inventory prose.
  - Focused tests, manifest artifact checks, and the required documentation workflow reject stale output, missing model/index routes, schema drift, invalid stability, and an untracked current route. The owner-reviewed native scope is pinned to 2,472 stable IDs, classifies 2,471 as revision-bound `experimental`, and keeps `Game.BreakIntoDebugger` exact `internal`; no reachable symbol is promoted to `stable` implicitly.
- `verified` - pinned external-project evidence and promotion inventory
  - `BuildTools/ExternalProjectEvidence.json` classifies 30 concerns and 180 exact source references from Last Frontier `50b8cb4` and TLA `b603d8f` as 24 promoted, 2 boundary-owned, 1 promotion candidate, and 3 project-owned decisions. Every record has an owner, priority, current or planned target, required reviews, and an artifact/test/policy promotion gate. Native debugging records Last Frontier's explicit loopback/enablement/static-check/crash-lane pattern and TLA's disabled/wildcard mismatch as negative evidence. AngelScript evidence contrasts Last Frontier's test-only namespace exceptions with TLA's broad production mutable-global allowlist. Mapper/viewer evidence pins Last Frontier's launch tasks, single-process headless preview script and generator, warmup/view plans, developer packaging, maintenance trigger, and layered model/particle acceptance while treating TLA's project-owned Mapper target as compatibility evidence and its missing focused-viewer/capture workflow as negative evidence; the AiControl convergence remains promoted only at the transport/security/lifecycle layer, while game schemas and MCP namespaces stay project-owned.
  - `BuildTools/docs_external_evidence.py` validates Engine targets and the eleven-owner review policy, generates the internal JSON/Markdown audit, and can prove all external paths against the pinned git trees even when the Last Frontier checkout is dirty. Focused tests, manifest freshness validation, and CI reject schema, owner, target, disposition, source, or output drift.
- `verified` - independent AngelScript style and refactoring guide
  - [AngelScript Style and Refactoring](../en/how-to/scripting/style-and-refactoring.md) derives namespace/file ownership, first-line sort and side-module construction, clang-format 20 plus AngelScript repairs, mutable-global and attributed-call constraints, nullable/invariant usage, generated-source ownership, refactoring classification, narrow batching, and validation from current Engine source and CoreScripts rather than a game migration plan.
  - `test_docs_angelscript_style.py` pins backend order and `0`/`1` side macros, every CoreScripts namespace, preprocessor balance, UTF-8/EOF shape, formatter nullable/template/array/named-argument repair, exact attribute/settings source, EN/RU fence parity, durable headings, manifest/site/AI routing, external-evidence promotion, and CI registration. `docs_validate.py` makes the focused suite mandatory.
  - Last Frontier and TLA remain evidence only for recurring practice and migration risk. Comment language, gameplay vocabulary, project generators, migration policy, quality thresholds, module catalogs, fixtures, and gameplay acceptance stay project-owned.
- `verified` - reusable gameplay and integration test harness
  - [GameplayTesting.md](../GameplayTesting.md) now owns boundary-based test selection, narrow-first proof, deterministic fixture rules, ordered process manifests, readiness/required/forbidden markers, one monotonic deadline, bounded cleanup, result codes, JSON reports, CI integration, and failure routing.
  - `BuildTools/gameplay_test_runner.py` is a revision-pinned helper CLI with positive, missing-marker, forbidden-marker, timeout/cleanup, malformed-manifest, unresolved-placeholder, and report tests under `test_gameplay_test_runner.py`; `Examples/GameplayTestHarness` supplies the fast synthetic fixture.
  - Minimal Multiplayer uses the same runner for content and server/client scenarios. The current Windows binaries pass baked metadata agreement, server startup, network connection, map load, remote calls, replicated state, item interaction, and clean shutdown. Script registries, project fixtures/assertions, filters, content ids, persistence backends, platform labs, and acceptance thresholds remain project-owned.
- `verified` - source-backed packaging and release procedure
  - [Packaging and Release](../en/how-to/release/packaging.md) separates build capability, packager capability, project qualification, and publication. It owns `DefinePackage`, build/bake/package ordering, Windows/Linux/Web/Android payloads, server/service/daemon boundaries, and the explicit absence of current macOS/iOS packager output.
  - The same guide defines artifact/provenance manifests, Windows and Android signing/secret boundaries, project acceptance evidence, release and rollback checklists, and failure routing. Generated package and support models remain the exhaustive contract owners.
  - `Examples/PackagingMatrix` now derives a complete Config-baker input from live Engine settings, builds Windows/Linux client/server/headless/service-or-daemon roles, checks archive/payload parity, runs packaged client/server through the updater handshake, and emits a revision/hash/inventory/runtime manifest. The Windows route is locally green; both routes and commit-addressed artifact uploads are required in `validate.yml`, with the first landed Linux run still pending.
  - `Examples/MinimalMultiplayer` now carries the same evidence shape in readable game source: a settings-derived complete config, Windows/Linux raw plus archive targets, payload/archive comparison, hashes, and packaged gameplay through the shared runner. Local Windows evidence covers two archives, 28 client files, 37 server files, and one passing interaction scenario; Linux and landed artifacts remain unobserved.
  - Last Frontier now routes to the Engine procedure while retaining concrete package rows, CI hosts, credentials, infrastructure, databases, and release acceptance. A remotely reachable exact example pin, observed Linux/immutable CI artifacts, release tag, and owner-authorized publication remain promotion gates.
- `verified` - security and secret-flow boundary
  - [Security and Secrets](../en/how-to/release/security-and-secrets.md) now owns the reusable threat/ownership split, config substitution timing, the narrow command-line masking guarantee, package-host signing handoff, CI isolation, secret-free artifact checks, rotation/revocation, incident routing, and project-owned provider boundary.
  - `package.py` now derives authored root plus selected sub-config for package-only values and resolves ordinary or target host directives at package time. Android passwords cross into Gradle only through dedicated environment variables; Windows signing receives only the binary path. `test_package_security.py` pins inheritance, fail-closed missing inputs, Android unbaked overlays, and Windows hook resolution.
  - Project credentials, secret-manager/provider selection, environments, account policy, operational retention, RPO/RTO, concrete recovery evidence, and actual incident decisions remain outside Engine. The reusable backup/recovery boundary is now owned by [Backup and Recovery](../en/how-to/release/backup-and-recovery.md).
- `verified` - reusable server release operations
  - [Release Operations](../en/how-to/release/operations.md) owns foreground/service/daemon selection, immutable release units, readiness and health evidence, preflight, staged rollout, graceful shutdown, rollback, failure routing, and a production qualification lane without importing a game's infrastructure.
  - Windows service startup failure now requests unsuccessful quit and joins the server thread before reporting `STOPPED`; registration stores the current command line exactly once with the consumed `--server-service-start` flag; the non-Windows daemon fails when `fork()` fails. `test_docs_release_operations.py` pins those source behaviors, lifecycle markers, the guide contract, manifest registration, and CI wiring.
  - Service definitions, product-specific service identity, topology, traffic drain, TLS/firewall, logs/alerts, SLOs, and incident authority remain project-owned.
- `verified` - backend-aware backup and recovery procedure
  - [Backup and Recovery](../en/how-to/release/backup-and-recovery.md) now owns JSON/SQLite/Mongo/Memory durable sets, the exact pending/committed recovery-oplog boundary, backup consistency levels, sidecar evidence, quiesced and proven-online capture, incident preservation, isolated restore, semantic read/write/restart checks, migration compatibility, and DR drills.
  - Recovery acceptance now includes prior-point size/count plausibility, an allowlisted isolated destination, dry-run/remap proof, and exact-name cleanup guards. `test_docs_backup_recovery.py` pins settings, JSON atomic replacement, SQLite `Storage.sqlite`/WAL/`synchronous=NORMAL`, Mongo URI ownership, oplog replay, guide structure, manifest registration, and CI wiring. Provider topology, schedules, retention, RPO/RTO, migrations, credentials, production data, and restore authority remain project-owned.

The initial documentation backlog plus the native-extension, script lifecycle/concurrency, prototype-format, map-format, model-format, text-format, effect-format, image-format, particle-format, font-format, audio, video, GUI runtime, AiControl transport/security/lifecycle, profiling, configuration/generated-content/upgrade workflows, packaging, security/secret-flow, release-operations and backup/recovery boundaries, support matrix, generated cross-domain public contract index, localization freshness infrastructure, AI delivery and deterministic evaluation, complete fenced-snippet parser coverage, source-owned teaching diagrams and Mapper/SPARK screenshots, locale-aware site navigation/search, version/locale/route, rendered-artifact and complete automated browser/WCAG validation, public-example governance and deterministic candidate materialization, first playable tutorial path, focused viewer and interactive Mapper/particle-authoring manuals, and dialog-ownership classification work are complete locally. The first five physical EN/RU tutorial pages, the architecture/source-tree/application group, the entity/client/server/maps/networking/persistence runtime groups, the scripting runtime/lifecycle/remote-call/method-ownership group, the exception-safety/local-variable/nullability/smart-pointer/thread-safety coding-contract group, the testing/gameplay-test/profiling quality group, the build/embedding/generated-content/project-configuration group, the BuildTools pipeline/Engine upgrade/support matrix/packaging group, the release-operations/security/recovery group, the translation/snippet/AI-evaluation documentation-quality group, the documentation-maintenance/site-publication operations group, the contract-change/ADR governance group, the Audio/Video guide and generated-reference group, the GUI Runtime guide and generated-reference group, the Text and Localization guide and generated-reference group, the AiControl Protocol guide and generated-reference group, the Native Extensions/project-dependency/ThirdParty guide and generated-reference group, the Effect Format guide and generated-reference group, the Image Format guide and generated-reference group, the Frontend and Rendering group, and every manifest-planned README entrypoint now prove stable-ID switching, Russian search, canonical locale routes, durable old pointers, and explicit paired entrypoint routing. The native-contract audit also removed project-owned analyzer claims from Engine authority, restored an accidentally truncated Nullability paragraph, aligned untrusted-client validation, and removed a stale `NOT_DESTROYING` read rule. The quality audit corrected stale test-app initialization from `InitApp(-1, nullptr)` to `InitAppForTesting()` and pins test-source, CMake, generated-inventory, sanitizer, coverage, process-runner, and Tracy contracts. The build-foundation audit pins the strict stage order, current application set, codegen dependencies, resource-pack schema, sub-config inheritance, and project-owned dialog boundary. The build/release audit removes three stale generic Editor CI entries and pins stage, contract-diff, support/package, and route ownership against live source models. The release-safety audit corrects malformed Windows service registration, pins real service/log/health behavior, and adds destination allowlisting plus cleanup guards to recovery procedure. The documentation-quality audit raises the source-owned deterministic retrieval threshold from 90 to 100 percent and pins 22 tasks, 50 retrieval checks, 61 answer checks, eleven snippet languages, parser ownership, translation freshness, and route ownership. The documentation-operations audit replaces a stale hand-maintained local test list with discovery plus CI ownership, corrects the physical locale and README counts, records the current 11-interaction/13-screenshot browser contract, and revalidates the GitHub Pages/domain boundary. The governance audit adds the missing AiControl protocol to the eighteen-domain change guide, repairs AI-delivery numbering, replaces stale first-group/README claims with generated ownership, and pins all seven canonical/legacy decision routes in CI. The media-reference locale audit moves both authored guides and all thirteen generated Audio/Video reference pages under canonical EN/RU routes, keeps every former route and stable entry anchor generated, and raises reviewed mirror coverage to 71/195. The GUI Runtime locale audit moves the authored runtime guide and all seven generated GUI references under canonical EN/RU routes, keeps every former heading and stable entry anchor, and raises reviewed mirror coverage to 79/195. The Text and Localization locale audit moves the authored guide and all six generated text-format references under canonical EN/RU routes, keeps every former heading and stable entry anchor, and raises reviewed mirror coverage to 86/195. The AiControl Protocol locale audit moves the authored guide and all six generated protocol references under canonical EN/RU routes, keeps every former heading and stable entry anchor, and raises reviewed mirror coverage to 93/195. The Native Extensions locale audit moves both authored integration guides, the ThirdParty maintenance guide, and all four generated native-extension references under canonical EN/RU routes, retains durable legacy headings and entry anchors, and raises reviewed mirror coverage to 100/195. The Effect Format locale audit moves the authored shader guide and all seven generated effect-format references under canonical EN/RU routes, retains every former heading and stable entry anchor, and raises reviewed mirror coverage to 108/195. The Image Format locale audit moves the authored image/sprite guide and all seven generated image-format references under canonical EN/RU routes, retains every former heading and stable entry anchor, and raises reviewed mirror coverage to 116/195. The Particle Format locale audit moves both authored particle guides and all eight generated particle-format references under canonical EN/RU routes, retains every former heading and stable entry anchor, and raises reviewed mirror coverage to 126/195. The Font Format locale audit moves the authored font guide and all eight generated font-format references under canonical EN/RU routes, retains every former heading and stable entry anchor, and raises reviewed mirror coverage to 135/195. The Map Format locale audit corrects stale bare-placement and single-map authoring claims, moves the authored map guide and all five generated map-format references under canonical EN/RU routes, retains every former heading and stable entry anchor, and raises reviewed mirror coverage to 141/195. The Model Format locale audit moves the authored model guide and all seven generated references under canonical EN/RU routes, corrects the baked particle-resource boundary and locale-relative animation links, retains all 32 former guide headings and 63 stable entry anchors, and raises reviewed mirror coverage to 149/195. The Prototype Format locale audit moves the authored guide and all four generated references under canonical EN/RU routes, retains all former guide/reference headings and 135 stable entry anchors, corrects ID/continuation and project type-selection claims, and raises reviewed mirror coverage to 154/195. The Baking Pipeline locale audit moves the source-backed explanation under canonical EN/RU routes, preserves all former H2/H3 routes through a durable pointer, corrects the live ModelInfo order and pack-composition contract, and raises reviewed mirror coverage to 155/195. The Frontend and Rendering locale audit adds the missing application rendering service, pins the exact backend and direct-Metal placeholder contracts, moves the source-backed explanation under canonical EN/RU routes, preserves every former H2/H3 route, and raises reviewed mirror coverage to 156/195. The starter's pinned Windows/Linux and current-Engine clean-runner lanes are green; the playable source and package routes are locally Windows-green, while their Linux lanes are wired but unobserved. Packaging is the sole remaining external-evidence promotion candidate. Finish the remaining production platform work by confirming landed documentation-site, contract-diff, and package artifacts and recording the actual Pages source branch/folder. The reviewed `fonline-project-template` candidate is source-staged privately, while `fonline-minimal-multiplayer` is source-ready in Engine but not staged remotely: published Engine `fee50fb63` lacks pending APIs and current validation/package source. Land a clean remotely reachable Engine revision first, then rematerialize, run pinned/current smoke and package checks, and push privately. Branch/security settings, cross-platform pinned/current evidence, immutable tags/artifacts, and explicit visibility transitions still gate publication. The remaining 35 Russian counterparts, same-domain clean Markdown aliases, broad non-internal stability review, manual production assistive-technology/zoom review, additional task-driven runtime/tutorial media, semantic example execution evidence, and independent model-family evaluation remain planned work. The audited Last Frontier `.fodlg` stack, GUI generator/editor, and game-specific AiControl schemas/actions/administrator tools remain intentionally project-owned until reusable implementations and tests are promoted.

The current physical locale total superseding the earlier aggregate paragraph is 195/195: Configuration and Data Sources plus Tools completed group 48 after the groups listed above. The manifest now enforces `complete`, CI invokes the explicit complete-parity gate, every current human stable ID has a canonical EN/RU pair, and both former flat routes remain durable pointers. The semantic overlay system is also complete: all 4,918 reader-facing strings across twenty generated domains have current reviewed records, including 2,474 native API descriptions and contract notes plus 221 map/prototype property projections. Its catalog, exact-source translation memory, and CI command now enforce `complete`, so newly exposed or changed generated prose cannot ship without the corresponding reviewed translation.

The current completion status superseding the earlier residual list has all 197 EN/RU pairs under complete-parity enforcement, all 2,472 native symbols described, and all native stability entries owner-reviewed: 2,471 are inventory-pinned `experimental` and `Game.BreakIntoDebugger` is exact `internal`. Future broad `stable` promotion depends on supported release lines rather than documentation completeness. Local Chromium now completes and visually verifies the Russian 200-percent reflow profile. The AI answer-completeness remediation and final two-family qualification exceed the unchanged 90-percent target. Remaining production evidence is landed GitHub Actions and Pages/domain proof, private example repositories rebuilt against a landed Engine revision with cross-platform evidence and release controls, and native-speaker plus production-domain zoom and representative assistive-technology review.

The current AI-evaluation total superseding the earlier aggregate paragraph is 27 tasks, 65 retrieval checks, and 92 answer checks. The AngelScript task pins source ordering and side macros separately from runtime initialization, plus the mutable-global compatibility boundary. The viewer task requires separate source/bake, focused-viewer, Mapper, representative-client, and retained-provenance evidence layers. The Mapper automation task separately pins save safety, warmed single-process batching, map-only versus full-window capture, pixel evidence, and Engine/project ownership. The build-interface task pins CMake override precedence, parser-backed main/helper CLI ownership, package-domain separation, and revision-pinned automation. The public-contract task requires callers to distinguish reachability from compatibility promises, retain source-owned scope/exact classification, follow source/model/reference ownership order, and pin exact Engine revisions. The package/support/examples task keeps valid package grammar, build-versus-smoke evidence, and private repository staging separate from production release proof.

The current AI qualification disposition is complete:

- Preserve the concise decision/checklist summaries, exact command and evidence boundaries, hidden rubric, objective repair limits, and focused tests as the maintained evaluation contract.
- Treat Qwen 27/27 and GPT-OSS 25/27 as the active qualification. The GPT-OSS viewer API invention and shallow adoption workflow remain explicit model-response failures, not reasons to weaken correct owner documentation.
- Rerun two materially different model families after task, owner, retrieval, prompt-construction, or material source changes; preserve exact input/harness hashes and compact reviews, and keep the 90-percent per-family target plus zero unsupported safety/migration/compatibility/release claims.

Group 43 moved the stock interactive Mapper manual and reusable Mapper automation guide to canonical EN/RU routes while retaining every former H2/H3 route in durable legacy pointers. The source audit added the previously undocumented `Ctrl+D` scroll-check and `Ctrl+B` blocked-hex commands, corrected the map-item freeze claim to the actual `SetTime(0.0f)` call, and pinned menus, layout storage/reset, lifecycle/entity/view exports, traversal protection, deferred full-window composition, the minimal capture fixture, canonical screenshot ownership, Last Frontier batch-preview practice, and TLA compatibility evidence in `test_docs_mapper_tools.py`.

Group 44 moved the human documentation home plus the generated CMake, main BuildTools CLI, and helper CLI references to canonical EN/RU routes. Generators retain every former H2/H3 and explicit stable-ID anchor under durable `Docs/generated/*` pointers, while current entrypoints route directly to locale owners. The source audit pins 44 CMake options, 10 stages, 6 selected helpers, 12 main commands with 24 arguments, and 9 helper programs with 16 subcommands and 74 arguments. Maintenance rules now require same-change EN generation, reviewed RU/hash reconciliation, legacy-route regeneration, localization, focused tests, and rendered-site validation after parser or interface changes.

Group 45 moved the seven generated native script API pages and the generated cross-domain public contract index to canonical EN/RU routes. `BuildTools/docs_reference.py` now emits 21 checked Markdown outputs and `BuildTools/docs_public_api.py` emits three; every former heading and explicit symbol/group anchor remains available through generated legacy pointers. The public contract links all eighteen machine domains, reports the live native inventory without promoting reachable symbols, and is now covered by a dedicated AI task. Russian fixed scaffolding, headings, tables, navigation, policy text, and links are reviewed and hash-pinned; source-authored symbol descriptions remain in their source language until the required stable-symbol-ID description catalog is implemented. The next direct locale group owns `GeneratedApiAndMetadata` and `Essentials`; package, generated support/public-example indexes, Configuration and Data Sources, and Tools remain after that.

Group 46 moved Generated API and Metadata plus Essentials to canonical EN/RU reference routes and retained every former H2/H3 through deterministic flat-path pointers. The source audit corrected the live 23-header Essentials dependency order, added `Threading.*`, `.inc`, Natvis, and CMake inventory coverage, and expanded the metadata guide across all eighteen generated contract domains, current documentation generators, evidence/media, localization, site, and AI delivery. Seven focused tests pin manifest/route ownership, translation hashes and fenced code, the exact umbrella/CMake inventory, all domain models, generator links, and AI routing. Localization is 186/195 (95.38 percent); the remaining nine pages are the five package references, generated support-matrix and public-example indexes, Configuration and Data Sources, and Tools.

Group 47 moved all five generated package references, the generated support matrix, and the generated public-example registry to canonical EN/RU routes. Their generators now emit reviewed English pages, hash-pinned Russian mirrors, and durable legacy pointers while retaining every former H2/H3 and explicit package entry anchor. Active README, guide, metadata, CMake, public-contract, evidence, site, and AI links route directly to locale owners; package public-contract generation and synthetic validator fixtures pin the new topology. Localization is 193/195 (98.97 percent), and only Configuration and Data Sources plus Tools remain before the full physical mirror gate.

Group 48 moved Configuration and Data Sources plus the Tools index to canonical EN/RU routes, updated all active locale-aware links, and retained complete H2/H3-compatible flat pointers. The source audit confirms default/config/sub-config/local-cache/command-line/auto-setting precedence, config/resource/cache/settings-storage ownership, all thirteen registered built-in bakers, and both current Mapper capture modes. Five focused tests pin source, manifest, locale, route, and CI contracts. Physical localization is 195/195 (100 percent), `localization.enforcement` is `complete`, and new required English pages cannot land without current Russian mirrors.

## Verified public example repository governance (2026-07-15)

- [PublicExampleRepositories.md](../PublicExampleRepositories.md) and ADR 0005 now own the four-repository portfolio, authority split, exact Engine pins, pinned/current compatibility lanes, release and tutorial tags, support/security boundaries, and asset provenance.
- `Examples/PublicRepositories.json` records four ordered source-ready repositories, owners, dependencies, checks, artifacts, asset policy, exit gates, and orthogonal remote visibility/staging state. The generated JSON/Markdown projection is consumed by site search and AI delivery and emits no URL for a private repository.
- `Examples/PublicRepositoryTemplate` supplies the checked line-ending/submodule policy, CODEOWNERS, pull-request, security, support, contribution, license, third-party, pinned/current workflow, metadata, README, and provenance templates used at publication.
- `BuildTools/docs_examples.py` rejects floating release refs, incomplete portfolio ownership, template drift, unresolved publication placeholders, non-exact revisions, gitlink/checkout mismatch, missing governance files, missing provenance files, and provenance digest mismatches. Its non-destructive staging mode copies only declared source, preserves the deep guide as `TUTORIAL.md`, applies the governance overlay, and pins metadata plus Engine-owned provenance URLs to one exact revision. Focused tests and standalone validation enforce deterministic output and CI wiring.
- The incoming updater protocol generation 2 and client host/runtime ABI 3 boundary is reflected in example release policy: incompatible frozen native hosts require a new full client package and one manual reinstall, never an in-process reload workaround.
- All four repositories were created privately on 2026-07-20. The template candidate is pushed as `source-staged`, with green pinned Windows/Linux and current-Engine workflows. The multiplayer remote remains reserved although its reviewed Engine source is ready: an isolated candidate pinned to published `fee50fb63` passed the structural gitlink/metadata/provenance contract but failed `BakeResources` on the unavailable `Game.RequestMapperWindowScreenshot` symbol, so it was correctly not pushed. Content Showcase is source-ready with locally green Windows native smoke/capture and isolated native-bake/Web-package/Chromium/WebGL 2 runtime evidence; Linux native/OpenGL and remote checks remain unobserved. Content Showcase and Native Extension Sample remain reserved remotely. Branch/security settings, release tags/artifacts, and public visibility remain administrator-authorized work, and no private repository is exposed as an already published source.
- `Examples/ContentShowcase` owns a standalone map/prototype/script gallery, thirteen deterministic project-original CC0 assets, byte-level provenance, explicit budgets, native process smoke, and checked Direct3D 11 and Chromium WebGL 2 capture contracts. The isolated `win64-showcase-smoke` and complete `web-showcase-runtime` route pass on Engine `fac978a67d1e601eb77389e8dc562d7e511705a0`; the Web route includes native baking, exact raw/ZIP package validation, native-server/browser lifecycle markers, and compositor pixels. Linux native/OpenGL, a remotely reachable clean Engine pin, remote pinned/current evidence, and release artifacts remain required.
